#include "monty.h"

/**
 * op_stack - function that sets the format of the
 * data to a stack (LIFO)
 * @stack_head: stack head
 * @count: line_number
 * Return: Nothing
*/
void op_stack(stack_t **stack_head, unsigned int count)
{
	(void)stack_head;
	(void)count;
	cis.op_flag = 0;
}
