#include "monty.h"
/**
 * queue - prints the top
 * @stack: stack head
 * @line_number: line_number
 * Return: no return
*/
void queue(stack_t **stack, unsigned int line_number)
{
        (void)stack;
        (void)line_number;
        bus.lifi = 1;
}
